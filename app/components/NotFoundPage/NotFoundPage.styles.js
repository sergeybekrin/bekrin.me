import { style } from 'typestyle';

export default {
    container: style({
        $debugName: 'container-404',
        flex: 1,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: '3em'
        // roboto-mono-ready('700')
    })
};
