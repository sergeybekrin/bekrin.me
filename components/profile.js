import styled from 'styled-components';

const Picture = styled.img`
  vertical-align: sub;
  border-radius: 50%;
  height: 2rem;
  width: 2rem;
`;

const Card = styled.div``;

export default { Card, Picture };
